

  <div class="field">
    <%= hidden_field :timepoint, :timepoint_study_id, :value => params[:study_id] %>
  </div>
  <div class="field">
    <%= hidden_field :timepoint, :timepoint_outcome_id, :value => @outcome.id %>
  </div>
  <div class="field">
    <%= text_field :timepoint, :timepoint_number %>
    <%= text_field :timepoint, :timepoint_time_unit %>
  </div>
  <div class="actions">
    <%= submit_tag "Add Time Point to Outcome" %>
  </div>