<%= nested_form_for(@population_characteristic, :remote => true, :update => "population_characteristics_table", :html=>{:id=>"edit_population_characteristic_form"},) do |f| %>
  <% if @population_characteristic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@population_characteristic.errors.count, "error") %> prohibited this population_characteristic from being saved:</h2>

      <ul>
      <% @population_characteristic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.hidden_field :study_id, :value => @study.id %>

<table class="form_table"><tr><td>
    <%= f.label :category_title %>:
    <%= f.text_field :category_title %>
</td></tr><tr><td>
<%= f.fields_for :population_characteristic_subcategories do |t| %>
  Subcategory: <%= t.text_field :subcategory %>
  Units: <%= t.text_field :units %>
  <%= t.link_to_remove "Remove" %><br/>
  <% end %><br/>
<%= f.link_to_add "Add a Subcategory", :population_characteristic_subcategories %>
</td></tr><tr><td>
    <%= f.label :notes_on_this_characteristic %>:<br/>
    <%= f.text_area :notes, :rows => 4, :cols => 40 %><br/><br/>
  <div class="actions">
		<button type="submit" class="positive">
			<%= image_tag "Add.png"%>Update Baseline Characteristic
		</button>
    <%= link_to 'Done Editing', new_study_population_characteristic_path(session[:study_id]), :remote=>true %>	
  </div>
  </td></tr></table>
<% end %>
