<div class="study_creation_header">
<h1>Editing Study Section: Design and Enrollment Details</h1>
</div>
<%= render "map" %>

<div id="population_characteristics_table">
<%= render 'design_detail_data_points/table' %>
</div>

<h3>Add Design Detail</h3>
<div id="population_characteristic_validation_message"></div>
<div id="population_characteristic_entry">
<%= render 'design_detail_fields/custom_field_form' %>
</div>
<br/>

<%= form_for(@study, :remote => true) do |f| %>
  <% if @study.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@study.errors.count, "error") %> prohibited this study from being saved:</h2>

      <ul>
      <% @study.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<% end %>




<br/>
<table id="bottom_nav"><tr><td align="left"><%= link_to "Back", "edit" %></td><td align="right">
<%= link_to "Continue", "attributes" %>
</td></tr></table>