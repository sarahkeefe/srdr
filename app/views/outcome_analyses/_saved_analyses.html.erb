<% unless @saved_analyses.empty? %>
  <strong>You have saved analyses for the following conditions:</strong><br/><br/>
  <table class='simple_table'>
  	<thead>
  		<th>Outcome</th><th>Subgroup</th><th>Time Point</th>
  	</thead>
  	<% @saved_analyses.each do |a| %>
  	  <tr>
  	  	<td><%= a[0] %></td>
  	  	<td><%= a[1].gsub("_"," ") %></td>
  	  	<td><%= a[2].gsub("_"," ") %></td>
  	  </tr>
  	<% end %>
	</table>
<% end %>
