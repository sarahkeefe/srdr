<%= form_for(@key_question, :remote => true, :html=>{:id=>"new_key_question"}) do |f| %>
  <% if @key_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@key_question.errors.count, "error") %> prohibited this key_question from being saved:</h2>

      <ul>
      <% @key_question.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table class="form_table">
  <tr><td>
    <%= f.label :question_text %>:</td><td>
    <%= f.text_field :question, :size => 50 %></td></tr><tr><td colspan="2">
    <%= f.label :question_description %>:</td></tr><tr><td colspan="2">
    <%= f.text_area :description, :cols => 50, :rows => 4 %>
  </td></tr><tr><td colspan="2">
  <div class="actions">
    <%= f.submit "Add Key Question" %>
  </div>
  </td></tr></table>
<% end %>
