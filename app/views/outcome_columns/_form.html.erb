<%= form_for(@outcome_column) do |f| %>
  <% if @outcome_column.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@outcome_column.errors.count, "error") %> prohibited this outcome_column from being saved:</h2>

      <ul>
      <% @outcome_column.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :outcome_id %><br />
    <%= f.text_field :outcome_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
