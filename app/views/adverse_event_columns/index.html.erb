<h1>Listing adverse_event_columns</h1>

<table>
  <tr>
    <th>Header</th>
    <th>Name</th>
    <th>Description</th>
    <th>Template</th>
    <th>Study</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @adverse_event_columns.each do |adverse_event_column| %>
  <tr>
    <td><%= adverse_event_column.header %></td>
    <td><%= adverse_event_column.name %></td>
    <td><%= adverse_event_column.description %></td>
    <td><%= adverse_event_column.template_id %></td>
    <td><%= adverse_event_column.study_id %></td>
    <td><%= link_to 'Show', adverse_event_column %></td>
    <td><%= link_to 'Edit', edit_adverse_event_column_path(adverse_event_column) %></td>
    <td><%= link_to 'Destroy', adverse_event_column, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Adverse event column', new_adverse_event_column_path %>
