<%= form_for(@primary_publication_number) do |f| %>
  <% if @primary_publication_number.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@primary_publication_number.errors.count, "error") %> prohibited this primary_publication_number from being saved:</h2>

      <ul>
      <% @primary_publication_number.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :primary_publication_id %><br />
    <%= f.text_field :primary_publication_id %>
  </div>
  <div class="field">
    <%= f.label :number %><br />
    <%= f.text_field :number %>
  </div>
  <div class="field">
    <%= f.label :number_type %><br />
    <%= f.text_field :number_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
