<%= form_for(@quality_aspect, :remote => true) do |f| %>
  <% if @quality_aspect.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quality_aspect.errors.count, "error") %> prohibited this quality_aspect from being saved:</h2>

      <ul>
      <% @quality_aspect.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :study_id, :value => params[:study_id] %>
  </div>
  <div class="field">
    <%= f.label :quality_dimension %>:
    <%= f.text_field :dimension %>
  </div>
  <div class="field">
    <%= f.label :value %>:
    <%= f.text_field :value %>
  </div>
  <div class="field">
    <%= f.label :notes %>:<br />
    <%= f.text_area :notes, :rows => 4, :cols => 40 %>
  </div>
  <div class="actions">
    <%= f.submit "Add Quality Aspect" %>
  </div>
<% end %>
