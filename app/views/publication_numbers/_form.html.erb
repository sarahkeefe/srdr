<%= form_for(@publication_number) do |f| %>
  <% if @publication_number.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@publication_number.errors.count, "error") %> prohibited this publication_number from being saved:</h2>

      <ul>
      <% @publication_number.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :publication_id %><br />
    <%= f.text_field :publication_id %>
  </div>
  <div class="field">
    <%= f.label :number %><br />
    <%= f.text_field :number %>
  </div>
  <div class="field">
    <%= f.label :number_type %><br />
    <%= f.text_field :number_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
